#include <stdio.h>
#include "bootLibGEV.h"

void gevDelete(char *name) {
    fprintf(stderr, "Sorry, this board has no GEV\n");
}

void gevShow(void) {
    fprintf(stderr, "Sorry, this board has no GEV\n");
}

void gevUpdate(const char *name, const char *value) {
    fprintf(stderr, "Sorry, this board has no GEV\n");
}
